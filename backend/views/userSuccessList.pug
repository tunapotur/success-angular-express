extends base
include _success-card

block content
  h1.page-header(class="mt-4 mb-8")= `${userSuccessList[0].user.name} ${userSuccessList[0].user.surname} Success List`
  .success-list
    each success in userSuccessList
      +success-card(success)